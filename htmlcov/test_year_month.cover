    1: import unittest
       
    1: from YearMonth import YearMonth
       
       
    2: class YearMonthTest(unittest.TestCase):
    1:     def test_given_valid_year_and_month_when_creating_year_month_then_store_them(self):
               # Given
    1:         year = 2026
    1:         month = 5
       
               # When
    1:         year_month = YearMonth(year, month)
       
               # Then
    1:         self.assertEqual(2026, year_month.year)
    1:         self.assertEqual(5, year_month.month)
       
    1:     def test_given_invalid_month_when_creating_year_month_then_raise_value_error(self):
    1:         test_cases = (0, 13)
       
    3:         for month in test_cases:
    2:             with self.subTest(month=month):
                       # Given
    2:                 year = 2026
       
                       # When / Then
    2:                 with self.assertRaisesRegex(ValueError, "month must be between 1 and 12"):
    4:                     YearMonth(year, month)
       
    1:     def test_given_non_integer_year_when_creating_year_month_then_raise_type_error(self):
    1:         test_cases = ("2026", True)
       
    3:         for year in test_cases:
    2:             with self.subTest(year=year):
                       # Given
    2:                 month = 5
       
                       # When / Then
    2:                 with self.assertRaisesRegex(TypeError, "year must be an integer"):
    4:                     YearMonth(year, month)
       
    1:     def test_given_non_integer_month_when_creating_year_month_then_raise_type_error(self):
    1:         test_cases = ("5", True)
       
    3:         for month in test_cases:
    2:             with self.subTest(month=month):
                       # Given
    2:                 year = 2026
       
                       # When / Then
    2:                 with self.assertRaisesRegex(TypeError, "month must be an integer"):
    4:                     YearMonth(year, month)
       
    1:     def test_given_year_month_when_rendering_repr_then_return_debug_friendly_text(self):
               # Given
    1:         year_month = YearMonth(2026, 5)
       
               # When
    1:         result = repr(year_month)
       
               # Then
    1:         self.assertEqual("YearMonth(year=2026, month=5)", result)
       
       
    1: if __name__ == "__main__":
>>>>>>     unittest.main()
